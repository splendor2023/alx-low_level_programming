#include "main.h"
/**
 * _strchr - entry point
 *@s: input
 *@c: input
 *Return: Always 0 (success)
 */
char *_strchr(char *s, char c)
{
	int j = 0;

	for (; s[j] >= '\0'; j++)
	{
	if (s[j] == c)
	return (&s[i]);
	}
	return (0)
}
